---
import { SEORenderer } from '@jjlmoya/utils-shared';
import type { SEOSection } from '../types';

interface Props {
  seoSections: SEOSection[];
}

const { seoSections } = Astro.props;
---

<SEORenderer sections={seoSections} />
